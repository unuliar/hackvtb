Query:
  type: object
  config:
    fields:
      user:
        type: User
        args:
          id:
            type: Int
        resolve: '@=resolver("App\\GraphQL\\Resolver\\UserResolver::resolve", [args["id"]])'